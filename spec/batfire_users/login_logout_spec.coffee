describe 'login', ->
  it 'uses the default provider', -> notImplemented()
  it 'takes a any provider', -> notImplemented()
  it 'validates provider in a given list', -> notImplemented()

describe 'logout', ->
  it 'empties App.currentUser', -> notImplemented()
