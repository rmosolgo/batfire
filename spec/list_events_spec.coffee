describe 'items_added', ->
  it 'adds to the loaded set', ->
    notImplemented()

describe 'items_removed', ->
  it 'removes from the loaded set', ->
    notImplemented()

describe 'items_changed', ->
  it 'updates items in the loaded set', ->
    notImplemented()
